USE mrqos;

drop table if exists pp_test;

create external table pp_test (
    ppip STRING,

    )

row format delimited
fields terminated by '\t'
lines terminated by '\n'
location '/ghostcache/hadoop/data/MRQOS/sandbox/pp_test00';